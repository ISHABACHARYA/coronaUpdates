import ThemeConfig from "./Theme.config";

export const darkMode = () => {
  return {
    type: ThemeConfig.DARK_MODE
  };
};
export const lightMode = () => {
  return {
    type: ThemeConfig.LIGHT_MODE
  };
};
